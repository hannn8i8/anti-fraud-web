# ── LINE Bot 金鑰設定範本 ──
# 使用方式：複製這個檔案成 .env，再把下面兩個值換成你自己的。
# 注意：.env 內含機密，已被 .gitignore 排除，千萬不要上傳到 GitHub。

# LINE channel 的「Channel access token（長期）」
# 取得位置：LINE Developers > 你的 channel > Messaging API 分頁
LINE_CHANNEL_ACCESS_TOKEN=在這裡貼上你的_access_token

# LINE channel 的「Channel secret」
# 取得位置：LINE Developers > 你的 channel > Basic settings 分頁
LINE_CHANNEL_SECRET=在這裡貼上你的_channel_secret
